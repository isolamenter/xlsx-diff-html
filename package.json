{
  "name": "xlsx-diff-html",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Browse Git xlsx diffs as HTML — CLI and Tauri desktop app.",
  "scripts": {
    "test": "node --test",
    "build:sidecar": "bash scripts/build-sidecar.sh",
    "build:sidecar:win": "powershell -ExecutionPolicy Bypass -File scripts/build-sidecar.ps1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "postject": "1.0.0-alpha.6"
  },
  "dependencies": {
    "daff": "1.4.2",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "engines": {
    "node": ">=20"
  }
}
